const Repo = (function(){
  var counter = 0

  return class {

    constructor(name, url, contributors){
      this.name = username;
      this.url = url;
      this.contributors = contributors;
      this.id = counter++
      store.repo.push(this);
    }

  }
}())
